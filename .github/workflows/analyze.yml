name: Threads 게시물 가이드라인 분석

on:
  workflow_dispatch:
    inputs:
      username:
        description: 'Threads 사용자 아이디 (@ 제외)'
        required: true
        default: 'just_followtax'
        type: string
      start_date:
        description: '시작일 (YYYY-MM-DD)'
        required: true
        default: '2024-01-01'
        type: string
      end_date:
        description: '종료일 (YYYY-MM-DD)'
        required: true
        default: '2025-12-31'
        type: string

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 의존성 설치
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Playwright 브라우저 설치
        run: playwright install chromium

      - name: 분석 실행
        env:
          THREADS_USERNAME: ${{ github.event.inputs.username }}
          START_DATE: ${{ github.event.inputs.start_date }}
          END_DATE: ${{ github.event.inputs.end_date }}
        run: |
          python src/main.py

      - name: 결과 업로드 (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: threads-analysis-${{ github.event.inputs.username }}-${{ github.run_number }}
          path: output/*
          retention-days: 30
        if: always()

      - name: 결과 요약 출력
        run: |
          echo "## 📊 분석 완료" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **사용자**: @${{ github.event.inputs.username }}" >> $GITHUB_STEP_SUMMARY
          echo "- **기간**: ${{ github.event.inputs.start_date }} ~ ${{ github.event.inputs.end_date }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f output/summary.txt ]; then
            cat output/summary.txt >> $GITHUB_STEP_SUMMARY
          fi
        if: always()
